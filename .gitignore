# Local session environment file — contains tokens, secrets, and environment URLs.
# NEVER commit this file.
.env.ps1

# VS Code workspace settings (machine-local)
.vscode/settings.json

# Build output
*.log

# Node dependencies are restored from package-lock.json.
node_modules/

# Temporary debug and review artifacts — entire folder ignored.
_review_tmp/

# Empty payloads root directory — canonical payloads are in scripts/payloads/
payloads/
